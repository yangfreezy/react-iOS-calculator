import React from "react";

import "./../styles.css";

const CalculatorLayout = ({ children }) => {
  return <div className="calculator-Layout"> {children} </div>;
};

export default CalculatorLayout;
